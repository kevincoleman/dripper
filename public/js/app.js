(function(){
  var app = angular.module('dripper', []);

  app.controller('DripperController', function(){

    var plants = {
      plant: {
        name: "Jean",
        interval: 8,
        type: "Pheonix"
      }
    };

  });
})();
